============
Installation
============

At the command line::

    $ pip install redflag

Or, if you use Conda environments::

    $ conda create -n redflag python=3.9
    $ conda activate redflag
    $ pip install goldilocks

If you want to help develop Redflag, read Development.
